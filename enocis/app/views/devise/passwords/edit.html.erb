<%= render '/layouts/admin_panel' do %>
  <div class="devise">
    <h2>Reseteaza parola</h2>
    <hr class="white">
    Lorem ipsum dolor sit amet consectetur adipisicing elit. Delectus quaerat provident nisi voluptas hic quia ad facere dolores eveniet! Fugit officiis odit pariatur reprehenderit quo consectetur reiciendis harum eaque. Magnam!
    <hr>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      
      <%= f.hidden_field :reset_password_token %>

      <div class="field form-group row">
        <div class="col-sm-4">
          <%= f.label :password, "Parola noua", class: " col-form-label" %>
        </div>
        <div class="col-sm-8">
          <%= f.password_field :password, autofocus: true, autocomplete: "new-password" %>
        </div> 
      </div>

      <div class="field form-group row">
        <div class="col-sm-4">
          <%= f.label :password_confirmation, "Confirma parola noua", class: "col-form-label" %>
        </div>
        <div class="col-sm-8">
          <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
        </div> 
      </div>

      <div class="form-group row">
        <div class="col-sm-4"></div>
        <div class="actions col-sm-6">
          <%= f.submit "Change my password" %>
        </div>
        <div class="actions col-sm-2 reset_psw">
          <%= render "devise/shared/links" %>
        </div>
      </div>
    <% end %>
  </div>
<% end %>