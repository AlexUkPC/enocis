<%= form_with(model: youtube) do |form| %>
  <% if youtube.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(youtube.errors.count, "error") %> prohibited this youtube from being saved:</h2>

      <ul>
        <% youtube.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="field">
    <%= form.label :service_id %>
    <%= form.text_field :service_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
